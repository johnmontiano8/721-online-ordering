export type OrderItem = {
  name: string;
  slug: string;
  qty: number;
  image: string;
  price: number;
  color: string;
  size: string;
};

export type Address = {
  firstName: string;
  lastName: string;
  address: string;
};
